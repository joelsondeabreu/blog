<div class="comment">
	<b><%= comment.name %></b> disse:<br/><%= link_to image_tag("delete.png"), post_comment_path(comment.post, comment), :method => :delete, :class => 'delete' %>
	<span>Postado: <%= time_ago_in_words comment.created_at %> atrás</span><br/><br/>
  <div class="body">
    <%= comment.body %>
  </div>
</div>